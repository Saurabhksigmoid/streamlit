apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "streamlit-app.fullname" . }}
  labels:
    {{- include "streamlit-app.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "streamlit-app.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "streamlit-app.name" . }}
    spec:
      containers:
      - name: {{ .Chart.Name }}
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        ports:
        - containerPort: 8501
